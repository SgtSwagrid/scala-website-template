## `resources/`

The `resources` directory is for the client's static assets, such as images and scripts.

**Warning:** Anything placed in this directory will be publicly available!

### Directory structure

The directory structure herein will be preserved in the URL path under `assets`,
so for example `resources/img/icon.png` will be available at `{URL}/assets/img/icon.png`.
